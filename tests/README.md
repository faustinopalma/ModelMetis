# Tests

Publication verification, September 19, 2026: the exact staged Git snapshot was exported to a temporary directory without ignored datasets, artifacts, the local console or deployment templates. All 104 Python tests passed in 15.87 seconds with no skips and the same two dependency warnings; Ruff passed. This used the existing installed test environment, not a fresh dependency installation. All 133 local links in the staged Markdown resolved within the published tree. No paid inference or deployment was repeated for publication.

The local audio-review suite is implemented in [test_audio_api.py](test_audio_api.py). It creates tiny synthetic PCM fixtures using the standard-library `wave` module; those fixtures are not motor-performance examples. It covers WAV canonicalization and PCM byte roundtrip/ranges, invalid/truncated/oversized/streamed input, opaque filenames, persistence across application instances, stale and concurrent revisions, real counts/search/filter/pagination, unavailable model semantics, liveness/readiness, and local-only request restrictions. Contamination tests insert known labels in RIFF metadata and a legacy reference column, confirm that the markers exist, and verify their exclusion from operational output and the audio-only model contract, including JSON-escaped forms. Console checks below record a separate local implementation, not included in this experimental-results publication; its npm commands require those unpublished local files.

September 17, 2026: 40 backend tests passed and Ruff passed on Python 3.13.13 ARM64. Five console tests and the TypeScript/Vite build passed on Node 24.15.0; npm audit reported 0 vulnerabilities after updating Vitest to 4.1.11. Two Python dependency deprecation warnings remain. These are local checks, not evidence of real teacher performance or Azure compatibility.

September 18, 2026: the latest full Python suite passed 104 tests in 14.85 seconds, with the same two dependency warnings and no skips; Ruff passed across all source, scripts and tests. The 64 [learning tests](test_learning.py) use synthetic audio and explicitly named fixture teachers, not a real model. They cover audio-only query inputs, silver targets deliberately opposite to sealed truth, actual scikit-learn `fit` interception, duplicate/group split rejection, abstention/error handling, escaped reference sentinels, exact evaluation coverage and ten training-entrypoint contamination cases. Tests reject reference fields, held-out training, altered provenance, duplicate/missing predictions and unknown statuses before fit; positive paths assert nonzero examples and artifacts. Install the `ml` extra for the learning suite; older tests may skip without scikit-learn, while later importer/adapter tests require it.

EXP-006 adds ordered one-audio/label-example-per-class payload checks, simulated-human support export and provenance, exact development-input preservation, source-hash selection, missing/duplicate classes, query ID/group/audio rejection, invalid origins/extra fields, pre-accounting failure, support-bound prompt hashes, silver-only fitting with demonstrations present, and aggregate support privacy. Raw and JSON-escaped sensitive sentinels are both checked. These are local accidental-leakage controls, not authenticated annotations or a filesystem sandbox. The separate eight-request real one-shot run validated endpoint acceptance but did not improve diagnostic accuracy; no paid call runs in pytest.

EXP-007 adds a synthetic Jin importer regression: twelve source groups are split by direction before windows, yielding 120 train/twelve development/four support clips; support/query overlap, checksum alteration, output overwrite, constant and nonfinite signals are checked. A separate supervised-control test intercepts the actual logistic-regression fit, verifies publisher training labels in this explicitly non-operational path and rejects held-out targets, mismatched audio hashes and query audio/group overlap. Operational silver training remains unchanged. These tests do not establish motor performance. The real four-control and twelve-call teacher experiment ran separately; aggregate results are in [the comparison](../ml/jin-directions-v1.json).

The generative-audio additions test the actual HTTP payload through `httpx.MockTransport`, byte-for-byte canonical audio, absence of IDs, strict response parsing, abstention, model-version/truncation checks, safe provider failures, conservative token pricing, persisted pre-request reservations, restart behavior, exclusive ledger locks, request limits, scheduling deadlines, incomplete reports, consumed-test rejection, exact teacher targets passed into real fitting, frozen prompts and nested acquisition snapshots. Later regressions cover actual token-provider construction, ARM version verification before accepting bare model names, known-tone/silence acoustic measurements, system/user role separation, rejected-text privacy, linear normalization without clipping, numerical context matching transmitted audio, constant-source exclusion, conflicting nonconstant labels, curve block classification and aggregate report reconciliation. None contacts an LLM. The separate x64 runtime passed a 58-package compatibility check and subsequently completed real Entra audio inference; see the live evidence rather than treating mocks as API proof.

Real measurements were run separately, not in pytest: the historical CLAP baseline audited 128 source pairs, compared two development prompts, collected 96 silver labels and evaluated one specialist on 16 final recordings. Subsequent actual GPT Audio experiments include synthetic capability, four Ottawa candidates, combustion-engine prompts, eleven silver labels, a 4/8/11 arrival curve and a normalization control. The [chronological report](../docs/EXPERIMENTS.md) and [aggregate evidence](../ml/audio-experiments-20260918.json) record failures and negative diagnostic outcomes. A passed boundary test does not make a teacher competent or enforce filesystem isolation. No Azure GPU scale-down was tested.

```powershell
$timer = [Diagnostics.Stopwatch]::StartNew(); try { .\.venv\Scripts\python.exe -m pytest tests/test_audio_api.py -q } finally { Write-Output "elapsed: $([math]::Round($timer.Elapsed.TotalSeconds, 1))s" }
$timer = [Diagnostics.Stopwatch]::StartNew(); try { .\.venv\Scripts\python.exe -m pytest tests/test_learning.py -q; if ($LASTEXITCODE) { throw 'Learning tests failed' } } finally { Write-Output "elapsed: $([math]::Round($timer.Elapsed.TotalSeconds, 1))s" }
$timer = [Diagnostics.Stopwatch]::StartNew(); try { .\.venv\Scripts\python.exe -m ruff check src/modelmetis tests/test_audio_api.py } finally { Write-Output "elapsed: $([math]::Round($timer.Elapsed.TotalSeconds, 1))s" }
$timer = [Diagnostics.Stopwatch]::StartNew(); try { npm run test --prefix apps/console } finally { Write-Output "elapsed: $([math]::Round($timer.Elapsed.TotalSeconds, 1))s" }
$timer = [Diagnostics.Stopwatch]::StartNew(); try { npm run build --prefix apps/console } finally { Write-Output "elapsed: $([math]::Round($timer.Elapsed.TotalSeconds, 1))s" }
```

Run from the repository root. Browser checks passed for upload and playback of a 2-second, 16 kHz synthetic PCM fixture, waveform correspondence (silent first half, nonzero second half), manual save, a competing-client revision, draft preservation, and synchronized queue/detail after conflict recovery. Light desktop (1440 x 1000) and light/dark mobile (390 x 844) had no horizontal overflow in the checked controls; screenshots were inspected. Persistence across application instances is covered by an API test. Complete keyboard traversal, all supported browser encodings, and exhaustive error-state visual checks remain unverified. No cloud result is established by these checks.

The broader lifecycle validation roadmap remains:

| Level | Scope | Dependencies |
| --- | --- | --- |
| Contract | Schemas, enums, versions, invalid inputs, and event compatibility | Local only |
| Unit | Routing, gates, revisions, idempotency, and transitions | Deterministic fakes |
| Local integration | Persistence, outbox, and concurrency | Isolated test database |
| Azure integration | Foundry, Blob, Service Bus, identity, and Azure ML | Authorized dev environment and limited budget |
| ML | Leakage-free splits, metrics, calibration, and bundles | Synthetic fixtures and small assets with valid usage rights |
| End-to-end | Teacher, collection, review, shadow, and rollback | Simulated locally; separate Azure run |
| Load and resilience | p95, throughput, 429, timeouts, backlog, and recovery | Dedicated environment, not production |

Fast tests require no secrets, GPUs, or metered calls. Live tests are explicit and do not run on external pull requests. No production images in fixtures. The final gold set is not a public fixture and is not used on every commit.

The [negative tests](../docs/VALIDAZIONE.md) are suite requirements. A test comparing zero examples must fail; mocks and simulations do not demonstrate compatibility with the real Azure API.
